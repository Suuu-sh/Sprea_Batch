import {expect,it,vi} from "vitest";import {ResearchApiClient} from "../src/shared/research-api-client.js";import {HttpClient} from "../src/shared/http-client.js";
it("posts the Phase 2 wire contract with Bearer authentication",async()=>{const fetcher=vi.fn(async()=>new Response(null,{status:202})),client=new ResearchApiClient("https://sprea.example", "secret",new HttpClient({fetcher}));await client.send("fixture",[{productName:"Item",condition:"new",price:100,buybackStatus:"accepting",fetchedAt:"2026-08-29T00:00:00Z"}]);const [url,init]=(fetcher.mock.calls as unknown as Array<[URL,RequestInit]>)[0];expect(String(url)).toBe("https://sprea.example/api/ingest/buyback-quotes");expect(new Headers(init.headers).get("authorization")).toBe("Bearer secret");expect(JSON.parse(String(init.body))).toMatchObject({provider:"fixture",sourceType:"scraper"});});
