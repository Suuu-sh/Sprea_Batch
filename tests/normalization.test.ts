import {describe,expect,it} from "vitest";import {normalizeCondition,normalizeJan,normalizeRawBuybackItem} from "../src/shared/normalization.js";
describe("collector normalization",()=>{it.each([["新品","new"],["未開封","new"],["新品未開封","new"],["未使用","unused"],["中古Aランク","used"],["判定不能","unknown"]] as const)("normalizes condition %s",(input,expected)=>expect(normalizeCondition(input)).toBe(expected));it("keeps only plausible JAN digits",()=>{expect(normalizeJan("JAN: 454-9995-648294")).toBe("4549995648294");expect(normalizeJan("123")).toBeUndefined();});it("normalizes the API contract without guessing missing identity",()=>expect(normalizeRawBuybackItem({productName:"  Item  Name ",jan:"bad",modelNumber:" X-1 ",condition:"未使用",price:"¥12,800",buybackStatus:"受付中",productUrl:"https://example.test/x",fetchedAt:"2026-08-29T00:00:00Z"})).toMatchObject({productName:"Item Name",jan:undefined,modelNumber:"X-1",condition:"unused",price:12800,buybackStatus:"accepting"}));it("rejects invalid price records",()=>expect(normalizeRawBuybackItem({productName:"x",price:"ASK",fetchedAt:"2026-08-29T00:00:00Z"})).toBeNull());});
