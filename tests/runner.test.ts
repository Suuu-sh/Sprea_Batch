import {describe,expect,it,vi} from "vitest";import {runCollectors} from "../src/runner.js";
describe("collector runner",()=>{it("continues after one collector fails",async()=>{const send=vi.fn(async()=>{}),good={name:"good",collect:vi.fn(async()=>[{productName:"Item",condition:"new" as const,price:100,buybackStatus:"accepting" as const,fetchedAt:"2026-08-29T00:00:00Z"}])},bad={name:"bad",collect:vi.fn(async()=>{throw new Error("offline");})};const results=await runCollectors([bad,good],send,()=>{});expect(results).toEqual([{name:"bad",ok:false,itemCount:0,error:"offline"},{name:"good",ok:true,itemCount:1}]);expect(send).toHaveBeenCalledWith("good",expect.any(Array));});});
