import type {BuybackCollector,RawBuybackItem} from "../../types.js";import {HttpClient} from "../../shared/http-client.js";import {parseMorimori} from "./parser.js";
export class MorimoriCollector implements BuybackCollector{readonly name="morimori";constructor(private readonly sourceUrl:string,private readonly http=new HttpClient()){}async collect():Promise<RawBuybackItem[]>{const response=await this.http.request(this.sourceUrl,{headers:{accept:"text/html"}});if(!response.ok)throw new Error(`morimori fetch failed (${response.status})`);return parseMorimori(await response.text(),this.sourceUrl,new Date().toISOString());}}
